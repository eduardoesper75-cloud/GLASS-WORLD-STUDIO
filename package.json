{
  "name": "gws-backend",
  "version": "0.1.0",
  "description": "Glass World Studio — Backend (Mes 1: Cimientos + Galaxia 3)",
  "scripts": {"start": "node dist/main"
    "start:dev": "nest start --watch",
    "build": "nest build",
    "start:prod": "node dist/main",
    "typeorm": "typeorm",
    "migration:run": "npm run build && typeorm migration:run --dataSource dist/database/data-source.js",
    "migration:revert": "npm run build && typeorm migration:revert --dataSource dist/database/data-source.js",
    "migration:show": "npm run build && typeorm migration:show --dataSource dist/database/data-source.js",
    "migration:generate": "npm run build && typeorm migration:generate --dataSource dist/database/data-source.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.0",
    "@nestjs/config": "^3.2.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/platform-express": "^10.4.0",
    "@nestjs/throttler": "^6.2.1",
    "@nestjs/typeorm": "^10.0.2",
    "bcrypt": "^5.1.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "helmet": "^7.1.0",
    "pg": "^8.12.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "speakeasy": "^2.0.0",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.5",
    "@types/bcrypt": "^5.0.2",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "@types/speakeasy": "^2.0.10",
    "typescript": "^5.5.0"
  }
}
